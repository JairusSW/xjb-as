name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install Dependencies
        run: bun install

      - name: Run Tests (simd/swar x ASC_SHRINK_LEVEL 0/1/2)
        run: bun run test

      - name: Verify against V8 (dtoa, dtoa-comp, ftoa, ftoa-comp)
        run: bun run verify
