// Package entry for AssemblyScript module resolution (`import { dtoa } from "xjb-as"`).
// Re-exports the public surface only — mirrors assembly/index.ts; keep in sync.
export { dtoa, dtoa_buffered } from "./assembly/dtoa";
export { ftoa, ftoa_buffered } from "./assembly/ftoa";
